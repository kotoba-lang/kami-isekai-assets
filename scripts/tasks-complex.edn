;; Hand-port backlog from bb.edn (ADR-2607173000). `:test` and `:render-test` have since been
;; ported to scripts/tasks.edn (see that file) — only `:gen-presets` (a one-off authoring script,
;; not a CI gate) remains unported.
{:gen-presets {:kind :complex, :doc "Write a curated preset pack of character/monster EDN files. --out <dir>", :form "(apply load-file [\"scripts/gen_presets.clj\"])"}}
